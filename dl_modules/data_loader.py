class generate_batch(object):
    """
    This is the last thing in the text-to-vectorized-minibatch pipeline
    this will wrap the DataLoader class in pytorch, which loads data in batches.
    """
    pass
